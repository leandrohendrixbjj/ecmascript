console.clear()

/*
   map() passada por cada elemento do Array e devolve
   um novo Array como resultado em cada elemento
*/
const data = [1, 2]

const sum = data.map(value => value + 1)

console.log(`Sum:${sum}`)
