console.clear()

// Retorna o MAIOR dos números passados como argumentos

console.log(Math.max(20, 0, 1, 2, 4, 5)) // 20

const data = [20, 0, 1, 2, 4, 5, 100]

console.log(Math.max(...data)) // 100
