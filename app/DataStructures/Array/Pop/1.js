console.clear();

// Removes the last element from an array and returns that value to the caller.
const arr = [1, 2];

let value = arr.pop();
console.log(`Value was removed:${value} - Array: ${arr}`);

