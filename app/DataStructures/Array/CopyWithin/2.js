console.clear();

let target = 0; // Posição para a qual os elementos serão copiados.
let start = 4; // Índice inicial de onde se copiará os elementos.

let arr = [1, 2, 3, 40, 50].copyWithin(target, start)

console.log(arr);