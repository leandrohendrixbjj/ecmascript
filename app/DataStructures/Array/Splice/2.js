console.clear();
// splice => Emendar

//Start = Indica de onde a alteração deve ser iniciada
//DeleteCount != 0 delete ifelse == 0 add
//Element = Valor que será add não obrigatório

const arr = ["angel", "clown", "mandarin", "surgeon"];

arr.splice(4, 0, 'I am used do it');

console.log(arr);